# Security policy

Please report credential exposure or a device-bricking risk privately through GitHub Security Advisories instead of a public issue.

This project does not require network credentials. Never attach Codex session files, device QR codes, MAC addresses, vendor APKs or firmware images to an issue.

The CLI intentionally has no firmware flashing command. Media writes should be tested only on owner-controlled hardware with stable power and a known recovery path.
